name: Sync rocket grunt quotes

on:
  schedule:
    - cron: "23 8 * * 1" # weekly at Monday 08:23 UTC — quotes change rarely
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      # Push as the Pushologist GitHub App so the commit can bypass the
      # branch protection on main (GITHUB_TOKEN pushes are rejected there).
      - name: Mint Pushologist app token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_SECRET }}
      - uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Fetch Team Rocket grunt quotes from PokeMiners
        run: npm run fetch-rocket-grunt-quotes
      # Any synced data can feed generated filter output, so the snapshot moves
      # with it. Idempotent: when the new data doesn't change any filter, the
      # fixture is unchanged and nothing extra gets committed. Doing this
      # unconditionally means nobody has to re-derive which data reaches which
      # filter every time the fixture definition changes.
      - name: Regenerate fixture snapshot
        run: npx vite-node scripts/generate-fixtures.mjs
      - name: Commit + push if anything changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- src/data/rocket-grunt-quotes.json src/__fixtures__/default-filter-output.json; then
            echo "No grunt-quote changes — skipping commit."
            exit 0
          fi
          git add src/data/rocket-grunt-quotes.json src/__fixtures__/default-filter-output.json
          git commit -m "chore(rocket): sync grunt quotes from PokeMiners"
          git push
